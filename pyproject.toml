[project]
name = "dscache"
version = "0.9.0"
description = "Record DeepSeek-style cached/missed prompt usage and analyze request tiers, prefix changes and cost scenarios in a local ledger."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "SuperMarioYL" }]
keywords = ["deepseek", "prefix-cache", "llm", "agent", "cost", "observability"]
classifiers = [
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: Apache Software License",
    "Topic :: Software Development :: Libraries",
    "Environment :: Console",
]
dependencies = [
    "httpx>=0.27,<0.28",
    "rich>=13.7,<14",
    "typer>=0.12,<0.13",
    # typer 0.12.x calls click's pre-8.2 make_metavar() signature; cap click so
    # `--help` doesn't crash on newer click. Transitive pin, not a new feature dep.
    "click>=8.1,<8.2",
]

[project.urls]
Homepage = "https://github.com/SuperMarioYL/dscache"
Repository = "https://github.com/SuperMarioYL/dscache"
Issues = "https://github.com/SuperMarioYL/dscache/issues"

[project.scripts]
dscache = "dscache.cli:app"

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dscache"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
